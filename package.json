{
  "name": "temperpack-js",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --outfile=dist/temperpack.js --minify",
    "dev": "esbuild src/main.js --bundle --format=iife --outfile=dist/temperpack.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
